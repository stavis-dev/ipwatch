
# 🌐 `ipwatch` — Утилита для наблюдения за публичным IP-адресом

> **Молчаливая, кроссплатформенная утилита, которая следит за твоим IP и сообщает — новый он или уже встречался.**

`ipwatch` — это простой, надёжный и **Unix-way** скрипт на Python, который:
- Получает твой текущий публичный IP и провайдера,
- Проверяет, не встречался ли он раньше,
- Сохраняет историю в **понятном CSV-файле в папке «Документы»**,
- Выводит красивый отчёт в терминал (если ты запустил вручную),
- **Возвращает код возврата `0` (новый IP) или `1` (уже был)** — для автоматизации.

> ✅ Работает на **Windows, Linux и macOS** — даже если Windows на русском языке!  
> ✅ Установка через `pip` — как настоящий CLI-инструмент.  
> ✅ **Никаких зависимостей** — только стандартная библиотека Python.

---

## 📌 Для кого этот скрипт?

- Ты **работаешь с мобильным интернетом**, где IP меняется при каждом подключении.
- Ты хочешь **знать, когда IP повторяется** — возможно, сессия не обновилась.
- Ты хочешь **автоматизировать действия** (например, перезапускать сервисы, отправлять уведомления).
- Ты ценишь **простоту, чистоту и Unix-философию**:  
  > *«Данные — в файле. Диагностика — в терминале. Управление — через exit code».*

---

## 📦 Установка

### 🔹 Установка через `pip` (рекомендуется)

```bash
pip install git+https://github.com/stavis-dev/ipwatch.git
```

или если у тебя локальная папка:

```bash
cd /path/to/ipwatch
pip install -e .
```

> 💡 `pip install -e .` — устанавливает в режиме разработки (editable), т.е. изменения в коде сразу применяются.

После установки можно запускать команду напрямую:

```bash
ipwatch -c "тест"
```

### 🔹 Установка через `pipx` (рекомендуется для Arch-систем)

Если ты используешь **Arch Linux** или другой дистрибутив на его основе, рекомендуется использовать `pipx` для установки `ipwatch`. Это изолирует приложение от системных пакетов и предотвратит конфликты.

1. Установи `pipx` через `pacman`:
   ```bash
   sudo pacman -S python-pipx
   ```

2. Установи `ipwatch` из текущей папки:
   ```bash
   pipx install -e .
   ```

Теперь ты можешь запускать `ipwatch` из любого места:
```bash
ipwatch -c "тест"
```

> 💡 `pipx` автоматически управляет виртуальным окружением и позволяет легко обновлять или удалять приложение:
> ```bash
> pipx uninstall ipwatch
> ```

### 🔹 Альтернативный способ: скачать и запустить

Если ты **не хочешь устанавливать**, можно скачать файл и запустить напрямую:

```bash
curl -O https://raw.githubusercontent.com/stavis-dev/ipwatch/main/ipwatch.py
python ipwatch.py -c "тест"
```

---

## 🚀 Использование

### 🔹 Простой запуск (с комментарием)

```bash
ipwatch -c "подключился к Wi-Fi"
```

📌 **Что произойдёт:**
- Скрипт получит твой IP и провайдера (через `ip-api.com`),
- Проверит, не встречался ли он раньше в `ipwatch_log.csv`,
- Выведет красивую таблицу в терминал (в `stderr`),
- Сохранит запись в файл `ipwatch_log.csv` в папке **«Документы»**:
  - **Windows**: `C:\Users\Имя\Documents\ipwatch_log.csv`
  - **macOS**: `~/Documents/ipwatch_log.csv`
  - **Linux**: `~/Documents/ipwatch_log.csv`
- Вернёт **exit code**:
  - `0` — IP **новый** ✅
  - `1` — IP **уже был** ⚠️

Пример вывода в терминале:
```
----------------------------------------
Timestamp           | IP Address    | ISP           | Comment
----------------------------------------
2025-11-11 14:30:00 | 192.0.2.1     | Mobile Operator | подключился к Wi-Fi
----------------------------------------

 ✅	 No IP matches
```

---

### 🔹 Проверка в скриптах (автоматизация)

```bash
#!/bin/bash
if ipwatch -c "автопроверка"; then
    echo "✅ Новый IP — запускаю синхронизацию..."
    rsync -av /my/data/ user@server:/backup/
else
    echo "⚠️ IP повторяется — возможно, сессия не обновилась."
fi
```

> 💡 `if ipwatch ...` — автоматически проверяет код возврата:  
> `0` → `then` — новый IP  
> `1` → `else` — IP уже был

---

### 🔹 Перенаправление вывода (для чистых данных)

```bash
ipwatch -c "тест" > output.txt
cat output.txt
# → файл ПУСТОЙ! (всё выводится в stderr)
```

→ `stdout` **никогда не загрязняется** — можно безопасно использовать в pipeline.

---

## 📂 Файл `ipwatch_log.csv`

Каждая запись сохраняется в файл `ipwatch_log.csv` в формате **табулированного CSV** — легко читается в любом текстовом редакторе (VS Code, Notepad++, Excel):

```
Timestamp	IP Address	ISP	Comment
2025-11-11 14:30:00	192.0.2.1	Mobile Operator	подключился к Wi-Fi
2025-11-11 14:35:00	192.0.2.1	Mobile Operator	вернулся домой
```

> ✅ Поддерживает **переносы строк** и **спецсимволы** в комментариях — CSV-парсер всё обработает.

---

## 🛠️ Опции

| Опция | Описание |
|-------|----------|
| `-c`, `--comment` | Добавить заметку к записи (например, `"выйду на улицу"`). |
| `--help` | Показать справку. |

Пример:
```bash
ipwatch --help
```

---

## 🧭 Как работает

1. Запрашивает IP и ISP через `http://ip-api.com/json/`.
2. **На Windows** — получает путь к папке «Документы» через реестр (`winreg`), чтобы работать даже на русской локали.
3. Сравнивает текущий IP со всеми записями в `ipwatch_log.csv`.
4. Если IP **новый** → пишет `✅ No IP matches` и возвращает `0`.
5. Если IP **уже был** → показывает совпадения и возвращает `1`.
6. Все **диагностические сообщения** идут в `stderr` — **`stdout` остаётся чистым**.
7. Все данные сохраняются в `ipwatch_log.csv` в папке **«Документы»** — **легко найти**.

---

## 📁 Структура проекта

```
ipwatch/
├── ipwatch.py          # основной скрипт
├── pyproject.toml      # метаданные и установка через pip
├── README.md           # это руководство
├── LICENSE             # лицензия (GPL)
└── .gitignore          # игнорируемые файлы
```

---

## 📎 Лицензия

**GPL** — свободно для использования, копирования и изменения.  
Ты можешь использовать его в коммерческих проектах.

---

## 💡 Советы

- Добавь в `cron` для автоматической проверки раз в час:
  ```bash
  0 * * * * ipwatch -c "автопроверка"
  ```

---

## 🙌 Благодарности

Спасибо за идею и вдохновение — этот скрипт создан с любезной помощью **ИИ Qwen** для тех, кто ценит **простоту, чистоту и контроль**. [Ссылка](https://chat.qwen.ai/) на чат.

---

> 📌 **Готово к использованию. Проверено на Windows (русская локаль), macOS и Linux.**  
> Теперь ты можешь быть уверен: **где бы ты ни был — твой IP всегда под контроле.**

